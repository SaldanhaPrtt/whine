import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexWrap: 'wrap',
    flexDirection: 'row',
  },
  label: {
    fontSize: 18,
    color: '#FFF',
  },
});

export default styles;